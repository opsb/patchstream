require 'spec_helper'

describe PatchStream do
	context "when a string field is changed" do
		before do
			Product.create name:
		end

		it "should generate a patch" do
			output.first.should == 
		end
	end
end